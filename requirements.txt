# Core quantum + math libraries
qiskit>=1.2.0
numpy>=1.26.0

# Optional audio generation (for live playback or MIDI output)
music21>=9.1.0
mido>=1.3.0
pyaudio>=0.2.13

# Optional machine learning + quantum remix extensions
tensorflow>=2.15.0
tensorflow-quantum>=0.7.3

# Optional visualization (for web or OpenGL rendering)
matplotlib>=3.8.0
pyopengl>=3.1.7
pygame>=2.6.0

# Development + formatting tools (optional but recommended)
black>=24.4.0
ruff>=0.5.0
pytest>=8.3.0
